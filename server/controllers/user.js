const { promisePool } = require("../config/dbConfig");

const getVideosFromSubscribedChannels = async (req, res) => {
try {
    
} catch (error) {
    
}
}
module.exports = { getVideosFromSubscribedChannels };
